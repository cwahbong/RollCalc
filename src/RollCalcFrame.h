#ifndef ROLL_CALC__ROLL_CALC_FRAME_H
#define ROLL_CALC__ROLL_CALC_FRAME_H

#include <wx/frame.h>

namespace roll_calc {

class RollCalcFrame: public wxFrame {
public:
  RollCalcFrame();
  virtual ~RollCalcFrame();
};

} // namespace roll_calc

#endif // ROLL_CALC__ROLL_CALC_FRAME_H
